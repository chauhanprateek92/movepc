# Move

A minimal Windows executable that simulates activity so the system doesn't lock or go to sleep.

Run "Move.exe" and it nudges the cursor once a minute for as long as it runs. There's no window or tray icon; stop it via Task Manager when you're done.
