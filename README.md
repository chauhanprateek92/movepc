# msteamspc

A minimal Windows executable that simulates activity so the system doesn't lock or go to sleep.

Run "msteamspc.exe" and it watches for 60 seconds of real inactivity, then nudges the cursor; any real mouse or keyboard activity keeps it dormant. There's no window or tray icon; stop it via Task Manager when you're done.
